import 'theme.imports.dart';

class AppTheme {
  const AppTheme();

  static ThemeColors colors = const ThemeColors();

  // static const ThemeTextStyles textStyles = ThemeTextStyles();

  // static ThemeWidgets widgets = ThemeWidgets();
}
